name: .NET Build and Test

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  DOTNET_VERSION: "9.x"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🛠 Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: 🧠 Display folder structure (debugging aid)
      run: ls -R

    - name: 💾 Cache NuGet packages
      uses: actions/cache@v3
      with:
        path: ~/.nuget/packages
        key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj') }}
        restore-keys: |
          nuget-${{ runner.os }}-

    - name: 📦 Restore dependencies
      run: dotnet restore ./DevHabit/DevHabit.sln

    - name: 🧱 Build solution
      run: dotnet build ./DevHabit/DevHabit.sln --configuration Release --no-restore

    - name: ✅ Run tests
      run: dotnet test ./DevHabit/DevHabit.sln --configuration Release --no-build --no-restore --logger trx --results-directory ./TestResults

    - name: 📤 Publish artifacts (optional)
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: ./TestResults

    - name: 🚀 Publish (build outputs)
      run: dotnet publish ./DevHabit/DevHabit.sln --configuration Release --no-build --no-restore --output ./publish
